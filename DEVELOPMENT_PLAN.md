# Development Plan

## Project

`大学文凭能有多文盲`

## Goal

做一个轻量、直接、带一点讽刺感的答题小游戏，用小学、初中、高中阶段本应属于“送分题”的基础题，测试大学生的基础能力是否已经明显退化。

当前阶段不追求题库规模，先把“像小游戏、能快速开始、能快速出结果”的闭环做稳。

## Current Product Direction

- 手机端优先
- 单屏卡片式界面
- 少说明、少装饰、少分栏
- 点击答案后快速自动进入下一题
- 界面支持主题切换，但题目流程与交互节奏保持一致

## Phase 1 Scope

- React + Vite 单页前端
- 极简首页
- 先选学科，再开始答题
- 题目全部为选择题
- 本地静态题库
- 选项点击后即时判定
- 约 `100ms` 后自动进入下一题
- 最后一题自动进入结果页
- 结果页展示得分和简单复盘

## Subjects For Phase 1

- 语文
- 数学
- 英语
- 物理
- 化学

## Data Strategy

第一版继续使用本地题库，按学科分组。

每道题字段统一为：
- `id`
- `subject`
- `prompt`
- `options`
- `answer`
- `explanation`
- `level`

## Implementation Priorities

1. 保持首页足够简洁
2. 保持答题页元素最少
3. 保证手机端显示正常
4. 保证答题节奏足够快
5. 继续扩充基础题题库

## Theme System Plan

目标是让界面可以切换以下三个参考主题，并且尽量贴近对应 HTML 原稿，而不是只做一套页面换色：

- `glass` 对齐 `gemini_新形态玻璃拟态.html`
- `kawaii` 对齐 `gemini_Kawaii.html`
- `anime` 对齐 `gemini_二次元.html`

主题切换的基本原则：

- 三个主题共用同一套 React 游戏流程
- 学科选择、答题、结算这三个页面阶段保持一致
- 题库、计分、自动跳题、结果复盘逻辑完全共用
- 视觉层允许主题专属差异，不强行压成一套纯变量皮肤

## Theme Compatibility Assessment

三个参考主题在产品结构上兼容，因为它们都遵循同一套小游戏骨架：

1. 首页选择学科
2. 答题页展示题目和选项
3. 结果页展示评价和重开按钮

真正的冲突不在流程，而在视觉实现方式：

- 背景机制不同：`glass` 是渐变加模糊 blob，`anime` 是点阵背景加星星和角色区，`kawaii` 是糖果色点阵加漂浮装饰
- 卡片体系不同：`glass` 是磨砂玻璃，`anime` 是粗黑边漫画框，`kawaii` 是奶油质感超大圆角卡片
- 按钮体系不同：三个主题的边框、阴影、圆角、按压反馈都不同
- 字体策略不同：`anime` 和 `kawaii` 需要更强的标题字体风格，`glass` 保持简洁现代
- 装饰元素不同：不能只靠现有统一背景块模拟全部效果

结论：

- 可以实现三个主题切换
- 不能依赖“只换颜色变量”的实现方式
- 需要“共享流程骨架 + 主题专属视觉层”的架构

## Theme Implementation Strategy

推荐实现方式：

1. `src/App.jsx` 继续负责统一的状态流和页面结构
2. 使用主题状态控制 `glass` `kawaii` `anime` 切换
3. 在样式层按 `data-theme` 分离三个主题的完整视觉规则
4. 单独提供主题背景/装饰容器，用于承载 blob、星星、漂浮元素、角色占位等专属内容
5. 用主题配置表统一管理每个主题的学科图标、强调色、标题文案和结果页文案

实现要求：

- 主题切换不能影响答题节奏和移动端单屏体验
- 新主题必须尽量对齐参考 HTML 的背景、按钮、颜色、阴影、圆角和标题气质
- 共用的是流程，不是把三个主题压成一套长得差不多的 UI

## Current Theme Implementation Status

当前代码已经完成第一轮主题系统落地：

- `glass` `kawaii` `anime` 三个主题已经接入界面切换
- `src/App.jsx` 已拆出主题配置、主题文案、主题学科图标和主题背景装饰容器
- `src/styles.css` 已按 `data-theme` 分离三套主题皮肤
- 题库、计分、自动跳题、结果页复盘仍然共用同一套 React 状态流

当前实现已经满足“可切换、可运行、结构正确”，但还没有完成逐像素级别的参考稿精修。

## Next Theme Work

下一步主题开发重点不是继续加功能，而是继续收紧与参考稿的视觉差距：

1. 优先精修 `glass`，继续对齐 `gemini_新形态玻璃拟态.html` 的卡片层级、背景范围和按钮状态
2. 精修 `kawaii` 的标题气质、奶油进度条、按钮压感和装饰位置
3. 精修 `anime` 的漫画边框、气泡标题、角色区与整体版式关系
4. 检查三套主题在首页、答题页、结果页三个阶段的切换一致性
5. 做移动端细修，避免装饰层挤压主体内容

## Build Validation Status

本轮开发已完成构建验证，但存在环境限制：

- 项目规则仍然要求优先使用 `pnpm build`
- 当前环境中 `pnpm` 不在 PATH
- `corepack pnpm build` 受网络限制无法拉取所需资源
- 已使用本地已有依赖通过 `cmd /c npm run build` 完成一次构建验证

后续在具备可用 `pnpm` 环境时，仍应补跑一次 `pnpm build`

## Theme Risks

- 如果继续沿用一套通用卡片样式，只改颜色，最终会和参考稿差距很大
- 如果不允许主题拥有少量专属装饰 DOM，背景和按钮细节无法还原
- 如果主题切换侵入太多业务逻辑，会让后续扩题和维护变复杂
- 如果每个主题各写一整套页面结构，后续会增加维护成本

## Product Tone

- 不是考试系统
- 不是知识站
- 是一个打开就能玩、很快就能看出水平的小游戏

## Risks

- 如果首页元素过多，会失去小游戏感
- 如果答题页文案过多，会拖慢节奏
- 如果题目太偏，会削弱“送分题也会错”的表达力度
- 如果手机端排版不稳，产品形态会直接失真

## Out Of Scope For Now

- 登录系统
- 排行榜
- 后端题库管理
- 多人对战
- 复杂动画系统
- 正式考试倒计时机制
