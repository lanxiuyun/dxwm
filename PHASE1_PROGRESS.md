# Phase 1 Progress

更新时间：2026-04-25

基于 [DEVELOPMENT_PLAN.md](D:/Users/MBri/Desktop/web_test/dxwm/DEVELOPMENT_PLAN.md) 的 `Phase 1 Scope`，这是当前项目的真实落地进度。

## Current Snapshot

- 当前题库仍然是本地静态数据。
- 主题库在 `src/data/questions.js`。
- 扩展题库在 `src/data/generatedQuestions.js`。
- 干净版知识点规划已补到 `src/data/knowledgePlans.js`。
- 路线图文档在 `KNOWLEDGE_POINT_ROADMAP.md`。
- 当前共 5 个学科：语文、数学、英语、物理、化学。
- 当前总题量已到 `1000` 题。
- 每个学科当前已到 `200` 题。
- 每个学科按 `小学 / 初中 / 高中 / 大学` 四档各 `50` 题的目标已经达成。
- 当前阶段的重点已经从“继续堆题量”切换为“知识点覆盖均衡、重复感控制、坏数据巡检”。

## Scope Status

### 1. 取消错误提示的红色动效

- 状态：`部分完成`
- 说明：答题过程里已经不再强调错误选项的红色高亮，但样式文件里仍可能残留旧状态样式，属于可继续清理的尾项。

### 2. 错题只记录一次且不可修改，命名为“错题本”

- 状态：`已完成`
- 说明：错题按题目 `id` 去重，只记录一次；结果页统一使用“错题本”展示。

### 3. 答完后只显示错题，不显示答对题

- 状态：`已完成`
- 说明：结果页只展示当前学科的错题列表，不再展示答对题列表。

### 4. 错题页加入滚动条

- 状态：`已完成`
- 说明：三套主题下的错题列表都已支持纵向滚动。

### 5. 题库做到每科 200 题

- 状态：`已完成`
- 说明：5 个学科都已经达到 `200` 题，总量 `1000` 题。

### 6. 每次从题库中按学科抽 10 道题回答

- 状态：`已完成`
- 说明：当前一轮固定抽取 `10` 题，并按学科与学段过滤后生成题组。

### 7. 已经答过的题不再重复

- 状态：`已完成`
- 说明：所有已作答题目都会写入本地存储；正常流程下后续抽题会排除。调试环境中保留了可重复返回和重开能力，便于测试。

### 8. 题目尽量简短

- 状态：`部分完成`
- 说明：整体方向已经维持在轻量、直接、快答题风格，但后续新增或修题仍需持续约束题干长度和复杂度。

### 9. 首页顶部显示总对题数、错题数、正确率

- 状态：`已完成`
- 说明：首页顶部已具备跨学科累计统计。

### 10. 错题本只显示当前学科的所有错题

- 状态：`已完成`
- 说明：结果页会按当前学科过滤错题本内容。

### 11. 选科后进入难度页，小学 / 初中 / 高中 / 大学

- 状态：`已完成`
- 说明：四档入口稳定可用，且每档当前都已补足到 `50` 题。

### 12. 增加左上角背景音乐按钮

- 状态：`已完成`
- 说明：背景音乐开关已放在左上角，并接入音频播放逻辑。

### 13. 玻璃风格改用更完整的 iOS 玻璃感方向

- 状态：`部分完成`
- 说明：当前视觉已经完成一轮材质与氛围调整，但仍有继续细修空间。

## What Changed Recently

- 题库合并方式已经改为稳定导出，修复了开发环境下“网站能跑，但题库像不是最新”的问题。
- 题库总量已补齐到每科 200 题，总计 1000 题。
- 首页和难度页已经加入开发态题库分布提示，便于快速看当前数据量。
- 结果页“返回大厅”已改成“再来一次”，并保留调试环境中的可回流能力。
- 新增了干净版知识点规划源文件 `src/data/knowledgePlans.js`，后续扩题优先按这份执行。

## Main Risks

- 当前主要风险不再是总量不足，而是知识点分布失衡导致的重复体感。
- 某些题型族仍可能偏密集，例如：
  - 语文：成语义项、文言词义
  - 数学：方程、集合符号、图像判断
  - 英语：连接词、被动语态、固定搭配
  - 物理：单位换算、电路基础、波与声
  - 化学：方程式判断、实验误差、平衡直觉
- `pnpm build` 目前仍有非语法型失败，开发调试以本地 `npm run dev` 为主。

## Recommended Next Steps

1. 按新的知识点池做人工抽样巡检，优先看“题目虽然不同，但体感很像”的区域。
2. 对高重复感知识点做压缩，对低覆盖知识点补题，保持每个学段更均衡。
3. 继续做坏数据检查，重点关注：
   - 题干近重复
   - 选项与答案不匹配
   - 学科或学段标注错误
   - 解释文案过长或跑题
4. 等用户继续试玩反馈后，再决定是否做下一轮定向补题或结果页细修。
