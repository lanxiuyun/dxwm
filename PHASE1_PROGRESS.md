# Phase 1 Progress

基于 [DEVELOPMENT_PLAN.md](D:/Users/MBri/Desktop/web_test/dxwm/DEVELOPMENT_PLAN.md) 的 `Phase 1 Scope`，对当前项目状态做的落地盘点。
更新日期：2026-04-25

## Current Snapshot

- 当前题库结构仍为本地静态数据，主题库在 `src/data/questions.js`
- 扩展题库放在 `src/data/generatedQuestions.js`
- 知识点规划文档放在 `KNOWLEDGE_POINT_ROADMAP.md`
- 当前共 `5` 个学科
- 当前每个学科已有 `105` 题
- 当前最终目标已明确为：每个学科在 `小学 / 初中 / 高中 / 大学` 四个难度下各做到 `50` 题，总计每科 `200` 题
- 当前扩题策略已从“单纯加题量”切换为“先补知识点覆盖，再补题量，并注意各知识点使用率均衡”

## Scope Status

### 1. 取消错误提示的红色动效
- 状态：`部分完成`
- 说明：答题交互里已经不再单独高亮错误选项，但样式文件里仍有旧红色状态残留，属于可继续清理的遗留项。

### 2. 错题只记录一次且不可修改，命名为“错题本”
- 状态：`已完成`
- 说明：错题按题目 `id` 去重，只记录一次；结果页统一使用“错题本”展示。

### 3. 答完后只显示错题，不显示答对题
- 状态：`已完成`
- 说明：结果页只展示当前学科的错题列表，不再展示答对题列表。

### 4. 错题页加入滚动条
- 状态：`已完成`
- 说明：三套主题下的错题列表都已支持纵向滚动。

### 5. 题库可以为 200 道易错题，每个学科 200 道
- 状态：`未完成`
- 说明：当前每个学科 `105` 题，距离每科 `200` 题还有明显差距；当前已经明确为四档难度各 `50` 题的目标结构。

### 6. 每次从题库中按学科抽 10 道题回答
- 状态：`已完成`
- 说明：当前一轮固定抽取 `10` 题，并按学科与难度过滤后生成题组。

### 7. 已经答错的不可重复答，已经答对的也不可重复答
- 状态：`已完成`
- 说明：所有已作答题目都会写入本地存储，后续抽题时统一排除；开发环境下已额外支持重复调试抽题。

### 8. 题目要求简单
- 状态：`部分完成`
- 说明：当前题目整体仍保持轻量、直接、偏送分题的方向，但后续扩题仍需继续约束题干长度、避免偏题和考试卷化。

### 9. 首页顶部显示总对题数、错题数、正确率
- 状态：`已完成`
- 说明：首页顶部已经有跨学科累计统计。

### 10. 错题本只显示当前学科的所有错题
- 状态：`已完成`
- 说明：结果页会按当前学科过滤错题本内容。

### 11. 选科后进入难度页，小学 / 初中 / 高中 / 大学
- 状态：`部分完成`
- 说明：难度选择页面和四档入口都已接入，且四档都已有稳定可玩内容；后续重点是把四档题量逐步补齐到每档 `50` 题。

### 12. 增加左上角背景音乐按钮
- 状态：`已完成`
- 说明：背景音乐开关已放在左上角，并接入音频播放逻辑。

### 13. 玻璃风格改用 iOS 玻璃特效风格
- 状态：`部分完成`
- 说明：玻璃主题已经完成一轮材质、浮层和背景光斑重构，但仍有继续精修空间。

## Main Risks

- Phase 1 当前的主要短板已经不在基础流程，而在题库规模与知识点覆盖
- 如果只追求总题量而不看知识点分布，容易出现题目重复度偏高的问题
- 如果继续扩题，需要持续遵守 `question-bank-guardrails`，避免题目变偏、变长、变成考试卷

## Recommended Next Steps

1. 继续按知识点缺口扩题，优先补 `初中 / 高中 / 大学` 三档，让三档尽快向每档 `50` 题靠拢。
2. 小学档也继续补到 `50` 题，最终形成每个学科 `4` 档各 `50` 题的 `200` 题结构。
3. 每轮补题后继续做坏数据检查，重点看重复 prompt、错误 subject、答案不在 options 中、知识点分布失衡。
